[package]
name = "host"
version = "0.1.0"
edition = "2021"

[features]
for_host = ["hound", "shared"]

[dependencies]
hound = { version = "3.5.1", optional = true }
shared = { path = "../shared", optional = true }

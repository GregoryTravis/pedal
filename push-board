(pushd board && \
  cargo build --release --lib --target thumbv7em-none-eabihf && \
  popd && \
  pushd lib/shim && make clean_exes && make PEDAL_MAIN=vibrato_main all && popd && \
  psh) 2>&1 | tee out
